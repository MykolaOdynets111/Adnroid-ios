package pages;

import io.appium.java_client.MobileElement;
import io.appium.java_client.android.AndroidDriver;

public class ChatTab extends BasePage{
    public ChatTab(AndroidDriver<MobileElement> androidDriver) {
        super(androidDriver);
    }
}
